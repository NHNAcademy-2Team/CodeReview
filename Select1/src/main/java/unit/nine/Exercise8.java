package nine;

public class Exercise8 {
    // Exercise 09.8 Define the exceptions that are necessary to catch the possible
    // errors that can occur in the class Matrix of Exercise 8.9.

    // * ExceptionWrongMatrixValues that is thrown in the method read if the data on
    // the file does not correspond to numeric values, or if the data are not
    // consistent with the form of a matrix (e.g., the rows have different length);
    // * ExceptionWrongMatrixDimension that is thrown in the method read if the data
    // on the file do not correspond to the dimension of the matrix.

    // Modify the class Matrix in such a way that it generates the new exceptions
    // when necessary.
}
